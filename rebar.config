{erl_opts, [debug_info]}.
{deps, [
  cowboy,
  jsone
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [cache]}
]}.

{relx, [
  {release, {cache, "0.1.0"}, [cache]},
  {dev_mode, true},
  % {extended_start_script, true}
  {include_erts, false}
]}.
